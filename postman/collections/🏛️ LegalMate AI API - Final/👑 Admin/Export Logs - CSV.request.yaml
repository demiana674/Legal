$kind: http-request
url: "{{base_url}}/api/admin/logs/export?AdminId={{adminId}}&format=csv"
method: GET
headers:
  Authorization: Bearer {{access_token}}
queryParams:
  AdminId: "{{adminId}}"
  format: csv
order: 12000
